import { css } from 'lit-element';
export default css`:host{display:block;transition:background 0.2s}:host(:hover){background:linear-gradient(to right, transparent, var(--color-hover), transparent)}
`;
