import { css } from 'lit-element';
export default css`:host{height:100%}:host div{height:calc(100% - var(--clip-height));background:var(--color-alt);opacity:0.5}:host([selected]) div{background-color:var(--color-text)}
`;
