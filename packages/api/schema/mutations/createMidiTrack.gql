mutation createMidiTrack($midiTrack: ECreateMidiTrack!){
    createMidiTrack(midiTrack: $midiTrack){
        id
        projectId
        name
        midiClips{
            clipId
            start
            duration
        }
        instrumentId
    }
}