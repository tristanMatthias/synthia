mutation createMidiTrack($track: ECreateMidiTrack!){
    createMidiTrack(track: $track){
        id
        projectId
        name
        midiClips{
            clipId
            start
        }
        instrumentId
    }
}